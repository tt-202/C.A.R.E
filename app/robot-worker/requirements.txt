firebase-admin>=6.5.0
google-cloud-firestore>=2.16.0

# Jetson hardware stack (install in venv after requirements.txt):
#   pip install -r requirements-jetson-hardware.txt
#
# MediaPipe FaceMesh needs protobuf 4.x (NOT 3.x or 5.x) on Jetson:
#   AttributeError: FieldDescriptor has no attribute 'label'
